{
  "name": "oudio-modal-beta",
  "version": "1.2.1",
  "description": "Gutenberg block – slide-in modal with iframe support",
  "scripts": {
    "build":   "wp-scripts build   --webpack-src-dir=src --output-path=build",
    "start":   "wp-scripts start   --webpack-src-dir=src --output-path=build",
    "lint:js": "wp-scripts lint-js src"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@wordpress/scripts": "^27.0.0"
  }
}
